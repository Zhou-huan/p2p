$(function(){
    $('.borrow-type').on('click',function(){
        //获取贷款的类型
        var type=$(this).data('type');
        //存入浏览器
        sessionStorage.setItem('type',type)
        //跳转到借款申请页面
       location.href='#/borrow/borrowapply'
    })
})